- defaults:
    link:
      relink: true

- clean: ["~"]

- link: {}

- shell:
    - [git submodule update --init --recursive, Updating submodules]
    - [cd brew && brew bundle, Updating HomeBrew environment]
